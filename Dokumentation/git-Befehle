### Beim ersten Mal
git init
git remote add origin https://gitlab.ldv.ei.tum.de/cpp-gk15/gruppe1_hauptprojekt.git
git pull

#######################
### Wichtige Befehle
#######################

# kann immer ausgeführt werden, gibt Überblick über den Status der Dateien
git status

# Neue Dateien und Änderungen herunterladen
git pull

########################
### Hochladen
########################

### Dateien im Remote-Repository löschen
git rm [-r] filename
    -> danach git commit und git push

### Lokal Dateien löschen, die nicht im remote git-Repo sind
git clean -n   (zeigt an, was gelöscht würde)
git clean -f   (löscht tatsächlich)

### pushen
# Dateien zum ändern vormerken
git add geaenderte_datei
# wenn Datei lokal gelöscht
git rm entfernte_datei

# immer mit git status überprüfen!!!!

# wenn verbockt
git reset

# wenn Status wie gewünscht
git commit -m "Funktion XY hinzugefügt"

# pushen, Änderungen werden hochgeladen, master=branch: erst hier wird was hochgeladen
git push -u origin master

########################
### Branches
########################

# Branch anlegen
git branch gameLogik

# In Branch wechseln
git checkout gameLogik
# danach pull -> überschreibt lokale Datein mit gameLogik, also vorher auf anderm Branch pushen
# jetzt kann ganz normal mit git rm/add/commit gearbeitet werden

# In Branch pushen
git push -u origin gameLogik

# In Branch master wechseln
git checkout master

# Master Branch mit gameLogik mergen
git merge gameLogik

# Überflüssige Branch gameLogik löschen (optional)
git push origin --delete gameLogik

# eigenen Branch nach merge auf den aktuellen Stand vom Master-Zweig bringen
git checkout xyz
get rebase origin/master

